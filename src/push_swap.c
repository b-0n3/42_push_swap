#include "push_swap.h"
void print_usage(void)
{
    ft_putstr();
    exit(1);
}

int main(int argc, char **argv)
{
    t_stacks stacks;

    if (argc < 2)
        print_usage();
    new_stacks(&stacks);
    
}